# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Project artifacts (large / regenerable / not tracked in git)
images/
test_output/
current/
.venv-scoring/

# Training data. Rebuilt by training/build_yolo_polygon.py and uploaded to
# Drive for Colab; nothing in the repo reads it. It was 540 MB of the 541 MB
# being cloned.
yolo_polygon/
stam polygon.coco/
stam_polygon_augmented/

# Build outputs, except the patch we hand to the company
*.zip
!dist/*.zip

# Secrets / credentials
hf_token
*_token
*.json.key
credentials*.json

# Personal Claude Code permissions — not the repo's business
.claude/settings.local.json
